{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cohort:memory",
  "title": "Cohort memory frontmatter (composed with shared)",
  "type": "object",
  "required": [],
  "properties": {
    "priority": {
      "type": "string",
      "enum": ["low", "normal", "high"],
      "default": "normal"
    }
  }
}
